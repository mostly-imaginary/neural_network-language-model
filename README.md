# Neural Network Language Model

A project focused on developing a Language Model that can run with fewer resource usage. The main idea is to create a Neural Network that will take in token emmbedings and produce a embedding which can then be transformed into a token. The idea is only experimental and might not produce the best results.